{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block edituser %}
    <h1>Edit User</h1>


    {{ include('user/_form.html.twig') }}

    {# {{ form_start(form, {'attr': {'id': 'new_edit_utilisateur'}}) }}
                <div class="row">
                        <div class="col-12">
                                {{ form_label(form.email) }}
                        </div>
                        <div class="col-12">
                                {{ form_label(form.password) }}
                        </div>
                        <div class="col-12">
                                <label for="verifpass">Saisir une seconde fois le mot de passe</label>
                                <input type="password" id="verifpass" required>
                        </div>
                </div>
                <button class="btn btn-success">{{ button_label|default('Update') }}</button>
        {{ form_end(form) }} #}
{% if is_granted('ROLE_ADMIN') %}
    <a href="{{ path('app_user_index') }}" class="btn btn-outline-warning mt-2">back to list</a>
{% else %}
    <a href="{{ path('rubrique') }}" class="btn btn-outline-warning mt-2">back to list</a>
{% endif %}

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
