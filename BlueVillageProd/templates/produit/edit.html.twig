{% extends 'base.html.twig' %}

{% block title %}Modifier{% endblock %}

{% block produit %}
    <h1>Modifier le produit</h1><hr>

<div id="test">
    {{ include('produit/_form.html.twig', {'button_label': 'Enregistrer'}) }}
    {{ include('produit/_delete_form.html.twig') }}
    <a href="{{ path('app_produit_show', {'id': produit.id}) }}" class="btn btn-outline-warning mt-2">Retour</a>
         {# {'id': produit.rubrique.id} le chemin complet pour le retour #}
</div>
    
{% endblock %}
