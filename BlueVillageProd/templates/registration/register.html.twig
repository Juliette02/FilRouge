{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block login %}
    
    <div class="container" id="bodylogin">

    <h1>Inscrivez-vous</h1>

    {{ form_start(registrationForm) }}

        {{ form_row(registrationForm.email) }}

        {{ form_row(registrationForm.plainPassword, {
            label: 'Mot de passe'
        }) }}  

        {{ form_row(registrationForm.nom, {
            label: 'Nom'
        }) }}

        {{ form_row(registrationForm.prenom, {
            label: 'Prenom'
        }) }}

        {{ form_row(registrationForm.adresseLivraison, {
            label: 'Adresse'
        }) }} 

        {{ form_row(registrationForm.cpLivraison, {
            label: 'Code Postal'
        }) }}  

        {{ form_row(registrationForm.villeLivraison, {
            label: 'Ville'
        }) }}         

        {{ form_row(registrationForm.adresseDifference, {
            label: 'L adresse de facture est elle differente ?'
        }) }} 

        <div id="Facture" style="display: none;">    

            {{ form_row(registrationForm.adresseFacture, {
                label: 'Adresse de facture',
            }) }}  

            {{ form_row(registrationForm.cpFacture, {
                label: 'Code postal Facture'
            }) }}

            {{ form_row(registrationForm.villeFacture, {
                label: 'Ville de facture'
            }) }}

        </div>

        {{ form_row(registrationForm.categorie, {
            label: 'Êtes vous professionnel ?'
        }) }} 

        <div id="Professionnelle" style="display: none;">

            {{ form_row(registrationForm.nomEntreprise, {
                label: 'Nom de l entreprise'
            }) }}
            
            {{ form_row(registrationForm.numeroSiret, {
                label: 'Numero de Siret'
            }) }}

        </div>
        
        <button type="submit" class="btn btn-primary">S'inscrire</button>
    {{ form_end(registrationForm) }} 
    
     Vous avez un compte ?<a href="{{ path('app_login') }}"> Connectez-vous !</a>

    </div>

{% endblock %}
{% block javascripts %}
    {# {{ encore_entry_script_tags('app') }} #}
    <script src="/js/register.js"></script>
{% endblock %}